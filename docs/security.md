# Безпека імпорту

Файли перевіряються за розміром, розширенням і MIME-типом, зберігаються поза публічною директорією з випадковим токеном та видаляються після обробки. Доступ до процесорів імпорту вимагає дозволу `newsletter_subscribers_manage` або прав sudo.
